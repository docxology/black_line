"""Version markers for the Black Line package.

The package version tracks the code surface; the registry digest (see
``serialization.registry_digest``) tracks the practice content. Reviewers can
pin both when citing an assessment.
"""

__version__ = "0.4.0"
